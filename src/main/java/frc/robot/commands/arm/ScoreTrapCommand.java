package frc.robot.commands.arm;

import frc.robot.subsystems.ArmSubsystem;

public class ScoreTrapCommand extends ArmBaseCommand {

    public ScoreTrapCommand(ArmSubsystem armSubsystem) {
        super(armSubsystem);
    }
}
